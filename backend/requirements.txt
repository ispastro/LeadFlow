# ============================================================
# LeadFlow RevOps Engine — Dependencies
# ============================================================

# --- Web framework ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.10.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6

# --- LangGraph + LangChain core ---
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.0
langchain-community>=0.2.0

# --- LangSmith observability ---
langsmith>=0.1.0

# --- Groq (direct client kept for non-LC paths) ---
groq>=0.4.1

# --- Embeddings (no torch dependency) ---
fastembed>=0.2.0

# --- Vector DB ---
qdrant-client>=1.7.0

# --- Database ---
psycopg2-binary>=2.9.0
asyncpg>=0.29.0
sqlalchemy>=2.0.0

# --- Auth ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- Utilities ---
python-dotenv==1.0.0
httpx>=0.27.0
email-validator>=2.0.0

# --- Text processing ---
beautifulsoup4==4.12.3
pypdf==4.0.1
python-docx>=1.1.0
